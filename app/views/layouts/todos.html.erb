<!DOCTYPE html>
<html>
  <head>
    <title>To Do App: <%= @page_title || "Admin Area" %></title>
    <!-- <link rel="stylesheet" type="text/css" href="app\assets\stylesheets\styles.css"> -->
    <%= stylesheet_link_tag('application', :media => 'all') %>
  </head>
  <body>

    <div id="header">
        <h1>To Do App</h1>
    </div>
      <div id="main">
        <!--  ***************  Check for any alerts, error or notices to be flashed *********************  -->
        <% if !flash[:alert].blank? %>
          <div class="alert">
            <%= flash[:alert] %>
          </div>
        <% end %>
        <% if !flash[:error].blank? %>
          <div class="error">
            <%= flash[:error] %>
          </div>
        <% end %>
        <% if !flash[:notice].blank? %>
          <div class="notice">
            <%= flash[:notice] %>
          </div>
        <% end %>

        <div id="content">
  <!-- before yield -->
        <%= yield %>
  <!-- after yield -->
        </div>
      </div>

  <div id="footer">
    <p id="copyright">&copy; green-leaf-services.com</p>
  </div>

  </body>
</html>
